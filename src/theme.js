// The palette lives in styles.css custom properties; this exports only the
// colors JS actually needs to paint (the LastGame win-probability SVG).
// Keep the two in sync if a value ever changes.
export const THEME = {
  CARDINAL: '#C5050C', // official UW cardinal
  INK_SOFT: 'rgba(31,26,23,0.62)',
  LINE: 'rgba(31,26,23,0.14)',
}
